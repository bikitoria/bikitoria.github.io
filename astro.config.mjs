import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://github.com/bikitoria/bikitoria.github.io.git/',
  base: '/', // for project pages, remove for username.github.io site
});